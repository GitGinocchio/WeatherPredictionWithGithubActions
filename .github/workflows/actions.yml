name: Update Spotify Stats

on:
  schedule:
    - cron: '0 0 * * *' # Esegui ogni giorno a mezzanotte UTC

  workflow_dispatch: # Permette l'avvio manuale del workflow

jobs:
  update-stats:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Use Spotify GitHub Action Stats
        uses: GitGinocchio/weather-prediction-github-action@main